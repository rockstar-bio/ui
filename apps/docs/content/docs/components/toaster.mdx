---
title: "Toaster"
description: "Gooey toast viewport wired to your theme — mount once, call toast() anywhere."
component: Toaster
source: ui/sonner.tsx
---

<ComponentPreview name="toast" />

## Usage

```tsx
import { Toaster } from "rockin/ui"

// Mount once in your root layout
export function Layout({ children }) {
  return (
    <>
      {children}
      <Toaster />
    </>
  )
}
```

## Anatomy

<Anatomy importCode={'import { gooeyToast, Toaster } from "rockin/ui"'} parts={["Toaster"]} sourcePath="ui/sonner.tsx" />

## Examples

<ComponentExample name="toast" title='Calling toasts' description='Use the `gooeyToast` API for success states and promises with loading states.' />

Import the trigger function as `import { gooeyToast as toast } from "rockin/ui"`. The viewport defaults to `bottom-right` with the close button at top-left; `theme` follows next-themes automatically. Rich colors and positioning can be overridden per Toaster.

## API Reference

<PropsTable component="Toaster" />
