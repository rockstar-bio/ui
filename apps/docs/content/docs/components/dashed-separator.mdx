---
title: "Dashed Separator"
description: "Responsive dashed divider that recomputes its dash count as the container resizes."
component: DashedSeparator
source: ui/dashed.tsx
---

<ComponentPreview name="dashed-separator" />

## Usage

```tsx
import { DashedSeparator } from "rockin/ui"

export function Example() {
  return <DashedSeparator className="my-4 text-border" />
}
```

## Anatomy

<Anatomy importCode={'import { DashedSeparator } from "rockin/ui"'} parts={["DashedSeparator"]} sourcePath="ui/dashed.tsx" />

Color comes from `currentColor` (default `text-border`). Tune `dashWidth`, `minimumGap` and `thickness` (px).

## API Reference

<PropsTable component="DashedSeparator" />
