---
title: "Apple Hello"
description: "Hand-drawn “hello” handwriting animation cycling through languages."
component: Apple
source: ui/apple/index.tsx
---

## Usage

```tsx
import { Apple } from "rockin/ui"

export function Hero() {
  return <Apple className="h-24" durationScale={1.2} />
}
```

## Anatomy

<Anatomy importCode={'import { Apple } from "rockin/ui"'} parts={["Apple"]} sourcePath="ui/apple/index.tsx" />

Self-cycling: when one language finishes drawing, the next starts. `durationScale` below 1 speeds the animation up, above 1 slows it down. Stroke inherits `currentColor`.
