---
title: "Dock"
description: "macOS-style floating dock of icon buttons with hover tooltips and springy motion."
component: Dock
source: ui/dock.tsx
---

<ComponentPreview name="dock" />

## Usage

```tsx
import { Dock } from "rockin/ui"
import { Home, Search, Settings } from "lucide-react"

export function Example() {
  const items = [
    { icon: Home, label: "Home", onClick: () => {} },
    { icon: Search, label: "Search", onClick: () => {} },
    { icon: Settings, label: "Settings", onClick: () => {} },
  ]
  return <Dock items={items} className="h-40" />
}
```

## Anatomy

<Anatomy importCode={'import { Dock } from "rockin/ui"'} parts={["Dock"]} sourcePath="ui/dock.tsx" />

The container reserves a fixed height (h-64 default) — override with `className`. Icons scale and lift on hover via `motion/react`; taps get press feedback.

## API Reference

<PropsTable component="Dock" />
