---
title: "Popover"
description: "Anchored floating panel for rich hover/click content."
component: Popover
source: ui/popover.tsx
---

<ComponentPreview name="popover" />

## Usage

```tsx
import { Button, Popover, PopoverContent, PopoverTrigger } from "rockin/ui"

export function Example() {
  return (
    <Popover>
      <PopoverTrigger render={<Button variant="outline" />}>What's new</PopoverTrigger>
      <PopoverContent className="w-72">Release notes go here.</PopoverContent>
    </Popover>
  )
}
```

## Anatomy

<Anatomy importCode={'import { Popover, PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger } from "rockin/ui"'} parts={["Popover"]} sourcePath="ui/popover.tsx" />

`PopoverContent` renders the portal and positioner; positioning props (`side`, `align`, `sideOffset`) live on it. Default width is fixed at `w-72` — override via className.

## API Reference

### Popover

<PropsTable component="Popover" />

### PopoverTrigger

<PropsTable component="PopoverTrigger" />

### PopoverContent

<PropsTable component="PopoverContent" />

### PopoverHeader

<PropsTable component="PopoverHeader" />

### PopoverTitle

<PropsTable component="PopoverTitle" />

### PopoverDescription

<PropsTable component="PopoverDescription" />
