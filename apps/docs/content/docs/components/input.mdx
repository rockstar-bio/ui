---
title: "Input"
description: "Text input with number-friendly defaults (decimal keypad, wheel blur)."
component: Input
source: ui/input.tsx
---

<ComponentPreview name="input" />

## Usage

```tsx
import { Input } from "rockin/ui"

export function Example() {
  return <Input type="email" placeholder="ada@lovelace.dev" />
}
```

## Anatomy

<Anatomy importCode={'import { Input } from "rockin/ui"'} parts={["Input"]} sourcePath="ui/input.tsx" />

## Examples

<ComponentExample name="input-with-label" title='With a label' description='Pair with Label and a hint line for accessible forms.' />

<ComponentExample name="input-states" title='States' description='Disabled, invalid (`aria-invalid`) and file inputs.' />

With `type="number"` the input forces `inputMode="decimal"`, hides native spinners and blurs on wheel so scrolling the page never mutates the value.

## API Reference

<PropsTable component="Input" />
