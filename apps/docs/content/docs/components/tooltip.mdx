---
title: "Tooltip"
description: "Hover/focus hint with an arrow and inverted colors."
component: Tooltip
source: ui/tooltip.tsx
---

<ComponentPreview name="tooltip" />

## Usage

```tsx
import { Tooltip, TooltipContent, TooltipTrigger } from "rockin/ui"

export function Example() {
  return (
    <Tooltip>
      <TooltipTrigger render={<Button variant="outline" size="icon-sm" />}>?</TooltipTrigger>
      <TooltipContent>3 unread notifications</TooltipContent>
    </Tooltip>
  )
}
```

## Anatomy

<Anatomy importCode={'import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "rockin/ui"'} parts={["Tooltip"]} sourcePath="ui/tooltip.tsx" />

Requires `TooltipProvider` mounted at the app root (`delay` defaults to 0). `Kbd` children get special treatment so shortcut hints align inside the bubble.

## API Reference

### Tooltip

<PropsTable component="Tooltip" />

### TooltipTrigger

<PropsTable component="TooltipTrigger" />

### TooltipContent

<PropsTable component="TooltipContent" />

### TooltipProvider

<PropsTable component="TooltipProvider" />
